import { run } from "../src/index";

describe("test index file", () => {
    it("should exist", () => {
        expect(run).toBeTruthy();
    });

    it("should exist 2", () => {
        expect(run).toBeTruthy();
    });
});
